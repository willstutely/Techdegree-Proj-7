import React from 'react';

// Simple component to render when an unmatched url is detected.
const UnmatchedURL = () => (
  <div className="not-found">
    <h2>Page Not Found</h2>
  </div>
);

export default UnmatchedURL;